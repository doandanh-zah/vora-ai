---
summary: "CLI reference for `vora clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `vora clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `vora clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `vora clawbot qr` (same behavior as [`vora qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `vora clawbot qr` -> `vora qr`
